"""
AdaptiAuth — ML Facial Verification Module

This module will contain:
- alignment.py   — Face detection (MTCNN) + 3D alignment preprocessing
- embedding.py   — CNN embedding model (MobileFaceNet-style, 128-dim)
- matching.py    — Cosine similarity matching against enrolled templates

Implementation in Phase 1.
"""
